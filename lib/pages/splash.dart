import 'package:flutter/cupertino.dart';import 'package:flutter/material.dart';import 'package:get/get.dart';import 'package:get/get_state_manager/src/rx_flutter/rx_obx_widget.dart';import 'package:loading_animations/loading_animations.dart';import 'package:nakhla_project/pages/Choices.dart';class Splash extends StatefulWidget {  const Splash({Key? key}) : super(key: key);  @override  State<Splash> createState() => _SplashState();}RxBool flag = false.obs;class _SplashState extends State<Splash> {  @override  void initState() {    // TODO: implement initState    super.initState();    print("هذا الدلي rfg اويت${flag.value}");    flag.value = true;    print("هذا الدلي dddd اويت${flag.value}");    Future.delayed( Duration(seconds: 5), () {      flag.value = false;      print("هذا الدلي بل اويت${flag.value}");      Get.offAll(Choices(),transition:Transition.noTransition,duration: Duration(seconds: 2));    });    print("هذا الدلي بعد اويت${flag.value}");  }  Widget build(BuildContext context) {    return Scaffold(      backgroundColor: Colors.white,      body: SafeArea(        child: Padding(          padding: const EdgeInsets.all(8.0),          child: Column(              children: [                ClipRRect(                    borderRadius: BorderRadius.circular(20),                    child: Container(                      width: 500,                      height: 500,                      decoration: BoxDecoration(                          color: Colors.white,                          borderRadius: BorderRadius.circular(85)),                      child: Image.asset("photo/nakhla.jpg")                    )),                Obx(                   () {                    return flag.value == true? LoadingFadingLine.circle(                        size: 100,                        backgroundColor: Color.fromRGBO(144, 195, 70, 0.9)                    ):Container();                  }                ),              ],          ),        ),      ),    );  }}